# Legacy workflows

Superseded by `H3_Seamless_Chain_v2` and `H3_Seamless_Chain_CORE` in the parent
folder. Kept so existing setups can still fetch them — every lane they had is
in v2 (the AIO's episode source, plate chain and audio spine were folded in,
and MEMORY had nothing v2 lacks).

They are **not maintained**: they predate the seamless chaining work, the
`context_pin` join, the anchor-input fixes and the dependency corrections in
2.1.1. Start from a v2 workflow.
